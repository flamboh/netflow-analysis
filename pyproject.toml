[project]
name = "netflow-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyarrow>=23.0.1",
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
]
